Title:
Client Profile API with Token Authentication

Description:
A simple Go web server that exposes a /user/profile endpoint, secured with token-based authentication middleware. The API retrieves client profile data from an in-memory database and demonstrates middleware usage for authentication in Go.
